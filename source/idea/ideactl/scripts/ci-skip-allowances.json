{
  "test/cli/binary-smoke.test.ts": "The release executables and archives are build outputs, absent until the distribution build has run.",
  "test/stacks/cutover-killers.test.ts": "A Java runtime is optional only in a checkout that declares it has no prerequisites.",
  "test/config/backup-continuity.test.ts": "Captured deployed templates are private and optional.",
  "test/parity/alternate-shapes.test.ts": "Private alternate-shape references are optional.",
  "test/cli/installer-params.test.ts": "Captured installer values are private and optional.",
  "test/config/jinja-branch-parity.test.ts": "The Python rendering oracle is optional only in a checkout that declares it has no prerequisites.",
  "test/stacks/retain-stateful.test.ts": "Captured deployed templates are private and optional; with none present there is nothing to itemise.",
  "test/parity/shape-matrix.test.ts": "Private alternate-shape references are optional.",
  "test/cli/upgrade-rehearsal.test.ts": "The captured development fixture is private and optional.",
  "test/stacks/cluster-config-db.test.ts": "The captured synchronization oracle and local database service are optional.",
  "test/cli/distribution.test.ts": "The two-target release build runs only on macOS; a public checkout on another platform cannot build it."
}
