# Built with `pip install -r requirements.txt ... --upgrade` (idea_code_asset.py),
# so an unpinned entry here silently bypasses requirements/idea-dev-lambda.txt.
# Keep in sync with that lock.
cryptography==50.0.0
