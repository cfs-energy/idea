# Begin: Disable ulimit
{%- if context.base_os in ('amazonlinux2', 'centos7', 'rhel7', 'rhel8', 'rocky8') %}
echo -e "
* hard memlock unlimited
* soft memlock unlimited
" >> /etc/security/limits.conf
{%- endif %}
# End: Disable ulimit
