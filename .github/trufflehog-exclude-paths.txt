# actions/checkout stores the runner token in .git/config
^\.git/
^venv/
^node_modules/
/node_modules/
^build/
# The AWS CLI installer archive and the directory it expands to, in case either is
# ever downloaded inside a checkout again.
^awscliv2\.zip$
^aws/
^dist/
__pycache__/
\.ruff_cache/
\.pytest_cache/
^source/idea/idea-cluster-manager/webapp/build/
# lockfiles carry git commit hashes that the GitHub token detector flags
yarn\.lock$
package-lock\.json$
